################################################################################

includes:
- ../app_locations.yml


################################################################################

# ---- Hiero

settings.tk-multi-snapshot.hiero:
  template_snapshot: show_hiero_snapshot
  template_work: show_hiero_work_file
  location: "@apps.tk-multi-snapshot.location"

################################################################################

# ---- Maya

# asset step
settings.tk-multi-snapshot.maya.asset_step:
  template_snapshot: asset_maya_snapshot
  template_work: asset_maya_work_file
  location: "@apps.tk-multi-snapshot.location"

# seq step
settings.tk-multi-snapshot.maya.seq_step:
  template_snapshot: seq_maya_snapshot
  template_work: seq_maya_work_file
  location: "@apps.tk-multi-snapshot.location"

# shot step
settings.tk-multi-snapshot.maya.shot_step:
  template_snapshot: shot_maya_snapshot
  template_work: shot_maya_work_file
  location: "@apps.tk-multi-snapshot.location"

################################################################################

# ---- Nuke

# asset step
settings.tk-multi-snapshot.nuke.asset_step:
  template_snapshot: asset_nuke_snapshot
  template_work: asset_nuke_work_file
  location: "@apps.tk-multi-snapshot.location"

# seq step
settings.tk-multi-snapshot.nuke.seq_step:
  template_snapshot: seq_nuke_snapshot
  template_work: seq_nuke_work_file
  location: "@apps.tk-multi-snapshot.location"

# shot step
settings.tk-multi-snapshot.nuke.shot_step:
  template_snapshot: shot_nuke_snapshot
  template_work: shot_nuke_work_file
  location: "@apps.tk-multi-snapshot.location"

################################################################################

# ---- Photoshop

# asset step
settings.tk-multi-snapshot.photoshop.asset_step:
  template_snapshot: asset_photoshop_snapshot
  template_work: asset_photoshop_work_file
  location: "@apps.tk-multi-snapshot.location"

# seq step
settings.tk-multi-snapshot.photoshop.seq_step:
  template_snapshot: seq_photoshop_snapshot
  template_work: seq_photoshop_work_file
  location: "@apps.tk-multi-snapshot.location"

# shot step
settings.tk-multi-snapshot.photoshop.shot_step:
  template_snapshot: shot_photoshop_snapshot
  template_work: shot_photoshop_work_file
  location: "@apps.tk-multi-snapshot.location"
